# Jest test paths known to be unrelated baseline failures for Ralph loop full-suite gate.
# One regex per line. Blank lines and lines starting with # are ignored.
# Add repo-specific baseline-failure regexes after install if needed.
